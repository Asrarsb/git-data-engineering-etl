# Git-Based Data Engineering ETL Project

## Steps to Run:
1. Install dependencies: pip install -r requirements.txt
2. Navigate to src folder: cd src
3. Run: python main.py

Output will be saved in data/cleaned_data.csv
